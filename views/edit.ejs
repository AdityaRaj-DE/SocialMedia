<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-white">
    <nav class="flex flex-row justify-center gap-2">
      <a class="px-4 py-3 text-lg text-[#101010] font-medium" href="/">Home</a>
      <a class="px-4 py-3 text-lg text-[#101010] font-medium" href="/profile">Profile</a>
      <a class="px-4 py-3 text-lg text-[#101010] font-medium" href="/logout">Logout</a>
    </nav>
    <div class="w-full min-h-screen p-10">
      
      <h5 class="mb-5 text-[#101010] text-3xl font-medium text-center">Edit your post</h5>
      <form action="/update/<%= post._id %>" method="post" enctype="multipart/form-data" class="flex flex-col justify-center items-center">
        <% if (post.photo && post.photo.data) { %>
          <img src="/post/image/<%= post._id %>" alt="Current Post Image" class="max-h-60 mb-2 rounded-lg gap-2" />
        <% } %>
        <textarea
          name="content"
          placeholder="what's on your mind"
          class="w-1/3 text-[#101010] p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-xl"
        >
        <%= post.content %>
      </textarea>
        <input type="file" name="photo" accept="image/*" class="block mt-2 bg-[#101010] px-4 py-2 text-white rounded-full" />
        <input
          type="submit"
          name=""
          id=""
          class="px-3 py-2 text-white font-bold bg-[#101010] block rounded-xl mt-2 w-40"
          value="update post"
        />
      </form>
    </div>
  </body>
</html>
